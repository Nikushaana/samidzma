import React from "react";

export const metadata = {
  title: "კატეგორიები | სამი ძმა",
  description: "პროდუქტების კატეგორიები",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}
